import { redirect } from 'next/navigation'

// Prototipo de panel nocturno de la era PartyMaps, retirado del aire (limpieza 30-08).
export default function PreviewPanelPage() {
  redirect('/inicio')
}
